{
  "name": "aoo_to_xlsx",
  "version": "2.0.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && esbuild --bundle src/index.ts --outdir=dist --minify --platform=node --packages=external",
    "test": "jest"
  },
  "devDependencies": {
    "@jest/globals": "^29.3.1",
    "esbuild": "^0.16.14",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "exceljs": "^4.3.0"
  },
  "keywords": [
    "array to xlsx",
    "xlsx",
    "excel",
    "json to xlsx",
    "json xlsx"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/asaje379/array_to_xlsx.git"
  }
}

